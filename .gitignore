.DS_Store
**/.DS_Store

# Campaign exports and order data. The orders Sheet carries customer names,
# emails and delivery addresses, and this repo is public (GitHub Pages).
# Analysis files belong outside the repo entirely — these patterns are only a
# safety net in case one is dropped in here by accident.
*.csv
*.tsv
*.xlsx
campaign-data/
analysis/

# Written by backend/stripe-setup.py — product/price/link IDs from a run.
# No keys in it, but it is a generated artifact, not source.
backend/stripe-setup-output.json
